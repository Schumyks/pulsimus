# STATE — Landing Pulsimus

> Estado vivo de **la landing de la agencia** (repo `pulsimus/`, deploy `pulsimus.vercel.app`).
> Fuente única de la verdad **volátil de la landing**. Actualizar al cierre de cada sesión que toque la landing.
> **Reglas de negocio / pipeline / metodología de la AGENCIA** viven un nivel arriba → [../../docs/STATE.md](../../docs/STATE.md).
> Migrado a su propio archivo el 2026-07-07 (antes vivía mezclado en el STATE raíz). Última actualización: **2026-07-24 21:05 (CIERRE — research 2 de MÉTODO: score REFORZADO anti reward-hacking + skill `asset-vectorizer` sellada a fondo + 2 parqueos)**. 🎯 Sesión de MÉTODO (cero construcción de scripts): tras el gate de la tienda, Alan pidió **endurecer la skill para CUALQUIER input** (composite = test de estrés A PROPÓSITO; **NO regenerar** para tapar baches del método). Hitos: **(1)** VERIFICADO que el **chequeo por CONTRASTE (diff visual) NO se usó** en la corrida final de la tienda (`piezas_vec/` solo score numérico; el `_trace_diff.png` vive solo en `pieces/` del intento de la mañana) → **causa raíz** de por qué los 3 errores del gate pasaron con score OK: el número dice CUÁNTO, el diff dice DÓNDE. **(2)** Marco **anti reward-hacking (Goodhart)**: un score de un eje (`mean |Δ|RGB`) es hackeable → catálogo trampas→defensa sellado. **(3)** Mini-research 2 (4 subagentes Sonnet paralelos, acotado 18%/40min → [`direccion/tecnicas-mapeo-verificacion-research.md`](direccion/tecnicas-mapeo-verificacion-research.md)): **score MULTI-EJE** (ΔE CIEDE2000 + SSIM + IoU silueta + FSIM/GMSD + Hausdorff · pooling **p95 no-mean** · región fijada por el TARGET · mide el gate, no el que dibuja) · **diff de contraste OBLIGATORIO por pieza** · **gate de riggeabilidad** (`xmllint --valid` caza la colisión id-elemento↔id-gradiente, VERIFICADO; gotcha `svgo cleanupIds` borra ids de rig) · **gate de recorte** (anillo perimetral transparente + mapa de residuo) · **separación de pegados = por COLOR/gradiente, NO por erosión** (corrige BL-21) · flag de confianza (PyMatting/entropy) · convergencia (early-stopping + target por clase). **(4)** Skill `asset-vectorizer` SELLADA a fondo (2 tandas de edits: post-gate + research 2; changelog con 2 entries y cómo revertir). **(5)** Backlog: **BL-21 corregido** + **BL-22 nuevo** (mapeo GPU Grounded-SAM, parqueado). Engram obs 387 (doctrina) + 388 (research 2). Commit `31d476d` = 1er sellado (skill post-gate); los edits de research 2 (skill+docs) van en el commit de ESTE cierre. — Previo: **2026-07-24 17:27 (CIERRE — corrida autónoma: TIENDA COMPLETA vectorizada como asset compuesto riggeable + skill afinada + norma base carita)**. 🎯 Sesión de método de vectorización en 3 bloques. **(1) TEST DEL MARCO**: carita redibujada de CERO con la skill → [`face_smile_3.svg`](../space-src/e0-supernova/piezas/spike-rive/face_smile_3.svg) **NORMA BASE** (score **8.31 en 1 sola pasada**; superó la vieja que tenía glow inflado 0.42 + borde inferior oscuro mal; viejas archivadas en `_archive/`). Reglas nuevas selladas en la skill: **calcar SOBRE EL PNG** (el SVG de referencia es solo vara de score, NUNCA copiar), **medir los 4 bordes del marco por separado** (depth asimétrico según la luz). **(2) SKILL `asset-vectorizer` AFINADA**: + `score.mjs` (métrica objetiva mean|Δ|RGB) + `assemble.mjs` + `reduce-palette.mjs`, sección **"asset COMPUESTO"** con 5 gotchas. **(3) CORRIDA AUTÓNOMA — TIENDA COMPLETA** (Alan al gym): primero **trace vtracer** ([`tienda_A_opt.svg`](../space-src/e0-supernova/piezas/spike-rive/tienda/tienda_A_opt.svg) 531KB, score 4.24, altísima fidelidad PERO **no riggeable** — el trace copia el grano; fusionar por color rompe el z-order y no baja peso, vive en la geometría). Veredicto de Alan: el trace "dibuja sobre el png", quiere **FORMAS geométricas limpias** (aplanar color + nitidez + riggeable). → Tienda ENTERA redibujada a mano como asset compuesto: mapeo de 16 elementos + base (fachada split+tablones / techo abovedado / cornisa / deck) + carita reubicada (transform medido por los ojos) + **6 subagentes en paralelo** (cartel STORE, puerta+ventana+haz, buzón, maceta+flores, sandwich+arbusto, toldos festón) con contrato cerrado + auto-verificación por score → ensamble z-order (`assemble.mjs`) → **[`tienda_vector.svg`](../space-src/e0-supernova/piezas/spike-rive/tienda/tienda_vector.svg) = 40KB, 31 grupos riggeables, score 18.6** (13× más liviano que el trace; el 18.6 > 4.24 es CORRECTO: formas limpias aplanan el grano a propósito). **ERROR mío destapado**: mapeé bboxes a ojo de vista reducida → corridos ~500px (5 agentes lo corrigieron midiendo). Docs: [`tienda/DESPIECE-vectorizacion.md`](../space-src/e0-supernova/piezas/spike-rive/tienda/DESPIECE-vectorizacion.md) + [`tienda/HALLAZGOS-corrida-tienda.md`](../space-src/e0-supernova/piezas/spike-rive/tienda/HALLAZGOS-corrida-tienda.md). TODO SIN COMMITEAR (skill+docs trackeables; assets en space-src gitignored). **GATE DE ALAN DADO** (primer run): macetas OK; carita = shortcut (reuso de face_smile_3, no vectorizada de la escena). **→ ARRANCÁ ACÁ (próxima — CORRECCIONES del gate)**: (1) **TECHO** mal = lo hice 1 plano con gradiente → rehacer con **≥5 secciones de tablón** (como la pared) **+ frente curvo con perspectiva** (no plano); (2) **TOLDOS** mal = izquierdo/festón tiene las **curvas AL REVÉS**, derecho **mal cortado + mal posicionado con el poste**; (3) **BUZÓN** = falta la **base oscura inferior** (mala selección de recorte). Insumo de proceso: **si Alan da los assets INDIVIDUALES ya recortados**, el trabajo por pieza sale mucho más sólido que mapear del composite. Detalle en [`tienda/HALLAZGOS-corrida-tienda.md`](../space-src/e0-supernova/piezas/spike-rive/tienda/HALLAZGOS-corrida-tienda.md) § Feedback. Decisión de uso abierta (trace 531KB vs vector 40KB vs híbrido). — Previo: **2026-07-24 14:52 (CIERRE — sesión de MÉTODO: research de vectorización + spike vtracer + MARCO DE CRITERIOS sellado + norma base)**. 🎯 Objetivo: subir la vectorización de assets de low-fi a **HIGH-FI bajo marca propia**. Hitos: (1) **Research deep (106 agentes → [`direccion/vectorizacion-research.md`](direccion/vectorizacion-research.md))**: DOS carriles → **A** `vtracer` (tracer de color MIT, corre en Linux, ELEGIDO) y **B** stack diferenciable `diffvg`/`LIVE`/`Bézier Splatting` (el "loop-hasta-score" como gradient descent — **PARQUEADO = BL-20**, es proyecto de ML, VRAM 8GB sin validar). Illustrator descartado (no corre en Linux, no paga Adobe). (2) **Spike vtracer sobre la carita/ventana**: trace crudo = ~95% fiel PERO 265 paths anónimos NO riggeable → el nudo es **ESTRUCTURA, no fidelidad**. Post-proceso construido: `segment2.mjs` (segmenta por color marco/cara/rasgos + connected-components) → vtracer bw por máscara → `assemble.mjs` (ensamble `id`s nombrados + gradiente muestreado del raster + primitivas para rasgos: ojos `<circle>`, boca bezier cúbico) → **`face_rig.svg` 30KB, 5 partes riggeables**. (3) **VEREDICTO de Alan (ojo de diseñador)**: para piezas SIMPLES/geométricas el redibujo **a mano `face_smile.svg` GANA** (tiene PROFUNDIDAD/bevels + NO tiene el gap negro que el trace mete entre paths); el trace híbrido es para piezas COMPLEJAS donde la mano no escala. **`face_smile.svg` = NORMA BASE (2.4KB)**. Aprendizaje: **el bevel es parte del DIBUJO, no luz por código** (error del spike descartarlo). (4) **MARCO DE CRITERIOS DE DECISIÓN sellado** en la skill `asset-vectorizer` (leer pieza → clasificar FORMA [primitiva/libre/textura] → clasificar RELLENO [plano/gradiente/bevel] → elegir método [simple=primitivas a mano · complejo=trace híbrido · orgánico=raster] → ensamblar riggeable; regla de oro: maximizar fidelidad del calco, NO la escala; deuda: pulir AMBOS procesos al límite). (5) Pipeline reacomodado (7 pasos + vectorización embebida + requisitos de entrada) — commit previo del día. **→ ARRANCÁ ACÁ (próxima — TEST DEL MARCO, pedido de Alan): arrancar de CERO con la ventana/carita usando el marco de criterios recién sellado, y medir en cuántas iteraciones se llega AL MENOS al mismo resultado que hoy (vara = `face_smile.svg`). Es el test de si los criterios guían bien al agente.** TODO SIN COMMITEAR (docs+skill del día; material del spike en `space-src/…/spike-rive/tienda/` gitignored). Handoff: `Handoffs/handoff-2026-07-24-1452.md`. — Previo: **2026-07-24 09:20 (CIERRE — spike de VECTORIZACIÓN raster→SVG validado + skill `asset-vectorizer` creada + pivote de calidad de assets)**. 🎯 Sesión de MÉTODO: convertir assets ilustrados (raster IA sobre verde) en **SVG vectorial riggeable** para alimentar Rive. Hitos: (1) **Intro-video E0 DESCARTADA** definitivamente (Alan: "no me gustó"); video = solo prototipado; archivada en `space-src/e0-supernova/descartes-intro-video/` (page.tsx/globals.css restaurados a HEAD). (2) **BL-18 Rive: cuenta creada por Alan**, spike en curso — guía [`escenas/e0-supernova/spike-rive.md`](escenas/e0-supernova/spike-rive.md). Frontera Rive: rígido (group+pivot, SIN huesos) vs deformación (mesh+bones); la tienda es todo rígido. (3) **Auto-slicer de sheets green-screen** OK (chroma `greenness=G−max(R,B)` + connected-components + **erode+feather** para el rim) → tienda-B `4940cf9f` en 26 piezas limpias `piezas/` (+ curado `piezas/spike-rive/`). erode+feather = paso PERMANENTE del recorte (regla en [`direccion/pipeline-assets.md`](direccion/pipeline-assets.md)). (4) **Flujo raster→SVG validado con la carita**: Alan generó `Ventana 2K fondo verde.png` (2K chroma puro) → `piezas/spike-rive/face_smile.svg` (2.2KB vs 3.4MB, riggeable). (5) **Método afinado por feedback de Alan (ojo de diseñador)**: leer PROMEDIANDO franjas (no 1 línea), rasgos por bbox, bandas planas vs divididas, sub-estructura fina (bevel=2 naranjas), y **calcar con overlay/diff** (`overlay.mjs` deja `_trace_over.png`+`_trace_diff.png` junto al SVG) en vez de medir a ciegas. (6) **Skill `asset-vectorizer` CREADA** (`.claude/skills/asset-vectorizer/`: SKILL.md + 4 scripts + specs-generacion.md) — en repo mientras se refina. **VEREDICTO de Alan**: el SVG "sirve, cumple la función", pero su ojo de diseñador quiere ELEVAR la fidelidad para no perderla con assets complejos; **pixel-perfect a mano NO escala** → el salto es automatizar el calcado (herramienta BL-19). **→ ARRANCÁ ACÁ (sesión nueva): PRUEBA con la TIENDA** (asset complejo) = stress-test del método `asset-vectorizer`. TODO SIN COMMITEAR (skill nueva + docs; assets en space-src gitignored). Handoff: `Handoffs/handoff-2026-07-24-0920.md`. — Previo: **2026-07-24 00:10 (CIERRE — PIVOTE de canal E0 a assets ilustrados + pipeline de assets definido + reorg de docs)**. ⛔ **Canal E0 VIDEO (Kling) y proto de partículas canvas DESCARTADOS por Alan** (pérdida de calidad + peso; con N escenas no escala). ▶️ **Dirección nueva: escenas armadas con los ASSETS ILUSTRADOS de Alan** (estilo Kurzgesagt, generados en Higgsfield/MJ) **recortados por chroma key y riggeados**. 🧩 **Pipeline de assets DEFINIDO** ([`direccion/pipeline-assets.md`](direccion/pipeline-assets.md)): frontera **🎨 ilustración (la genera Alan sobre fondo verde) / ⚡ efecto de código (lo dibuja Claude: trazo, luz, partículas, EKG)**; animación por **rig de piezas, NO frames**; + **prompts de asset sheet** (despiece / variaciones, con la corrección de la estrella: *mismo objeto, no objetos distintos*). 📐 **Despiece E0** en [`escenas/e0-supernova/despiece.md`](escenas/e0-supernova/despiece.md). 🗂️ **REORG de docs/ COMPLETA** (árbol vivo `direccion`/`escenas`/`quien-soy` + `archivo`, `README.md` mapa + 4 reglas de mantenimiento; ~69 links reparados, verificados 0 rotos) → commit `1de4ad0` (**SIN PUSHEAR**). 🌿 **Rama `f3-pulse-proto` BORRADA** (local+remota; 5 commits del 'latido enfermo' incompatibles; la idea EKG ya vive en el guión). 🎨 **Alan GENERÓ asset sheets sobre verde** (nano-banana): **tienda desglosada `4940cf9f` (EXCELENTE**: building+techo+toldo+cartel+ventana-carita😊×2+puerta+deck), kit mixto `ad4f1f79`, nebulosa `b2096494`; **regenerar: estrella** (salieron estrellas distintas → prompt B corregido) **y nave `6eb913c0`** (tiene suelo → pedir `no ground`). 🎛️ **BL-19 parqueado**: editor visual de coreografía (drag+path+play) para tunear a mano. ⚙️ Server dev en **:3001** (:3000 colgado, matado). **→ ARRANCÁ ACÁ (mañana): armar la 1ª escena E0 con assets — (1) bajar por MCP tienda-B `4940cf9f`; (2) auto-slicer (chroma key `#00B140` + connected components + defringe + descartar labels) → piezas con alpha a `space-src/e0-supernova/piezas/`; (3) montaje + micro-anim (ventana-carita respirando). Evaluar BL-19 ANTES/en paralelo. Insumos de Alan: regenerar estrella (prompt B) + nave (sin suelo).** — Previo: **2026-07-23 00:30 (CIERRE sesión Opus autónoma — E0 gateado punta a punta + intro-video montada)**. 🎬 Gate E0 cerrado en cadena: **(a) look ancla** = boards E0 de Alan (flat noche+ámbar, carita 😊→X_X) confirmados (pipeline MJ-base→HF-varía-estilo); **(b) canal** = VIDEO todo el arco + wordmark/copy DOM encima (mudo), clímax canvas jubilado; **modelo = KLING v3.0** (A/B ganado: keyframe inicio/fin nativo + 5x más barato que Seedance —8.75 vs 45 cr/clip— y calidad suficiente). 🎥 **Alan generó los 5 clips con Kling** (mudos, ~1080p, 4s c/u) → `space-src/Escena 0/Clips/`; QA director OK (consistentes al canon; el escape/despegue es el más flojo pero aceptable, NO se regenera). 🧰 Skills: instalada global `kling-3-prompting` (metodología pura); `openmontage/seedance-2-0` descartada+desinstalada (framework-bound OpenMontage, requiere API keys ajenas). **Convención: third-party→global, repo limpio**; comandos en el doc. 📄 Doc nuevo [`bl17-e0-video.md`](escenas/e0-supernova/video.md) (1 por escena: modelos, prompts EN por clip, keyframes inicio/fin, 1080p). 🏗️ **Intro-video MONTADA (autónoma, `next build` ✓ limpio, SIN commitear — gate de Alan)**: `public/space/intro/e0.mp4` (5 clips concat 1920×1080, 4.3MB de 38MB crudos) + poster/nebula webp; **`IntroVideo.tsx`** (motor a `<video>`, replica 1:1 los gates del canvas —1×/sesión, ?intro, reduced, skip, revealHero, fade—, wordmark DOM al final, fallback onError/onEnded); **`NebulaBackground.tsx`** (nebulosa remanente de fondo, aislada, NO toca SkyLayer, z-9 + screen blend); page.tsx switcheado (canvas `Intro.tsx` PRESERVADO, import comentado para volver); globals.css += keyframe px-nebula-drift + reduced-motion. **→ ARRANCÁ ACÁ: gate VISUAL de la intro-video (`npm run dev` + `?intro`) + resolver pendientes: (1) TIMING ~20s es largo p/autoplay —acortar/acelerar/recortar—; (2) COPY por acto (voz de Alan, TODO marcado en IntroVideo); (3) "STORE" en el cartel (defecto de texto); (4) render 9:16 mobile; (5) nebulosa fina (intensidad/posición/global-vs-Z1). Handoff completo: `Handoffs/handoff-2026-07-23-0030.md`.** — Previo: **2026-07-22 19:15 (CIERRE sesión Fable — día de DIRECCIÓN pura, cero código del sitio: OpenSpec `bl17-escenas` formalizado · boceto E0 GATEADO con física corregida · dirección nueva de Alan en E3/E5/E6/quien-soy · doc de assets · storyboard de 31 cuadros CON los boards E0 ya generados por Alan en Midjourney)**. ⚙️ **OpenSpec `bl17-escenas` creado y validado** (`openspec/changes/bl17-escenas/`: proposal + design D1–D8 + specs `motor-escenas`/`escenas-narrativas` con las 8 escenas como requirements + tasks en 9 grupos con gates 🚧 de Alan; deuda del 21/07 saldada). 📐 **Boceto E0 GATEADO en estructura** ([`bl17-boceto-e0.md`](escenas/e0-supernova/boceto.md)) **+ física corregida por dirección de Alan**: la intro actual cuenta un NACIMIENTO (acreción) y la escena 0 es una MUERTE — se adapta el motor (estrella preexistente → desestabilización → colapso/infall succionando la tienda → inhale [que ES la física real] → explosión existente aprobada) y **la nebulosa remanente PERSISTE como fondo del sitio** (handoff a Z1). Canal de producción ABIERTO: canvas vs video (recomendación híbrida en el addendum; spike = animatic MJ). 🧭 **Dirección de Alan 22/07 sellada en cadena** (guión+plan+specs+backlog): **E3** = descenso COMPLETO con la nave (atmósfera→nubes→tierra) · **E5** = beat de crecimiento (entran MÁS objetos en órbita) · **E6** = mission control INTERACTIVO (acciones del visitante avanzan el proceso en la tienda) y ALOJA el switch de skins (BL-16 trigger mudado) · **quien-soy → PÁGINA APARTE** linkeada del nav (BL-13 replanteado; demos redistribuidas: skin→E6, fichas Construcción→candidato E4/E5, toggle Calidad→a decidir). 🎬 **Pipeline de VIDEO como asset habilitado** (matiz del descarte del 18/07: aquello era video como motor de scroll; intro autoplay y micro-clips de personaje SÍ valen). 🎨 **[`bl17-assets-direccion.md`](direccion/assets-direccion.md)** (doc paralelo de generación: model sheets primero, inventario por escena, 7 reglas de video, tanda E0) y 🎞️ **[`bl17-storyboard.md`](direccion/storyboard.md)** (31 cuadros VEMOS/MOVIMIENTO/COPY/PROMPT + sección Midjourney con la mecánica aprendida a los golpes: sujeto-primero, image-prompt≠style-reference [gotcha que devolvió pixel art], bloque de estilo destilado vía `/describe`, 2 modos exploración/producción). **Alan GENERÓ los boards de E0 en MJ y están BUENÍSIMOS**: tienda con ventana sonriente (ancla de estilo), colapso desaturado con ventana X_X (el arco de la carita: sonríe→muere, narrativa visual gratis), nave+mostrador escapando con estela-EKG, supernova con la estrella de 4 puntas en el núcleo, nebulosa remanente — **E0 visualmente cubierto**; defecto conocido: "STORE" se cuela en carteles (pulir con `blank sign` en finales). ⚠️ **Engram DESCONECTADO toda la sesión** → resumen NO guardado en memoria; este STATE + `Handoffs/handoff-2026-07-22-1915.md` llevan todo — **backfillear `mem_session_summary` en la próxima apertura**. — Previo: **2026-07-21 23:40 (CIERRE sesión Fable — plan de operación GATEADO, B0+B1 ejecutados, MERGE A MAIN autorizado: el sitio nuevo está EN PRODUCCIÓN)**. 🧭 **Feedback de Alan sellado en memoria GLOBAL** (~/.claude/agent-context.md + Engram obs 357): plan documentado ANTES de operar, siempre — la sesión anterior quemó cuota de Fable ejecutando sin dirección escrita. 📋 **Plan de operación BL-17** ([`bl17-plan-operacion.md`](direccion/plan-operacion.md)) **GATEADO por Alan en dos rondas** con dirección ampliada: esto es CAMBIO DE ARQUITECTURA (supersede "capa aditiva" del 18/07); principio **"lo nuevo manda"** (prohibido punto medio tibio); producción en **ORDEN NARRATIVO E0→E7** con destino por sección decidido en el boceto de cada escena. Decisiones narrativas nuevas (también en el guión): **E2/Dolores se fusionan — asteroides DESTRUIBLES point&click para avanzar**; **E4: el demo mostrador se representa EN escena** (alien pide → dueño recibe; supersede La Espiga standalone); E6 tablero se reencuadra (maquinaria queda); quien-soy se adapta; **mobile DECIDIDO: misma narrativa, forma libre por escena**. ✂️ **B0 triage ejecutado** (`5737fe7`): decoración de validación RECHAZADA y borrada; base = estrella-cursor + pulso + reveal, sin nubes; `public/space/` shippeado; `public/clouds/` fuera del repo. 🐛 **Fix estrella corrida del cursor** (`1a2ab73`): canvas de Sky/Star sin tamaño CSS → con DPR fraccional (Linux 125%) todo se dibujaba en x·1.25 (dato clave de QA de Alan: error creciente hacia la derecha); fix `h-full w-full` como ya tenía la intro; + chase 3.5→8 (estrella pegada al cursor). 🚀 **MERGE A MAIN `1a2ab73` con autorización EXPLÍCITA de Alan** ("Mergealo tranquilo. autorizado") — **prod verificado live en pulsimus.vercel.app Y pulsimus.dk**; quien-soy P1 entró a prod con foto placeholder (Alan va a buscar una buena foto; "mientras tanto está bien"). ⚙️ **B1 motor de escenas HECHO** (`0484124`, delegado a ejecutor Sonnet 62k con spec cerrada + QA director): `app/components/motion/scrollEngine.ts` = único reloj de scroll (Lenis singleton refcount + ScrollTrigger registrado + `lenis.on('scroll', ScrollTrigger.update)`); StarLayer lo consume; `@gsap/react` instalado sin usar; regresión cero verificada (desktop/mobile/reduced, 0 errores). 📌 Deuda declarada: formalizar cambio OpenSpec `bl17-escenas` al arrancar B2 (el plan lo pedía al arrancar B1; se difirió una noche con causa: B1 tenía spec cerrada propia). 🔭 **Post-cierre (madrugada 22/07, pedido de Alan): 6 referencias analizadas EN PROFUNDIDAD** (fan-out de 6 Sonnet con browser real, ~700k de su cuota) → síntesis del director en [`bl17-referencias-analisis.md`](direccion/referencias.md): 7 lecciones transversales (scroll libre>secuestro valida nuestro stack; color como narrador; escena maestra continua>N pins; pacing con números de Seed; interacción con fallback de igual peso para E2; metáfora hasta la UI), 8 recetas técnicas con dueño (morph clase+delays de Species = la receta de cards→asteroides; reveal dos capas y color por data-attribute de Artifex — que usa NUESTRO stack exacto), mapa de aplicación POR ESCENA E0–E7, advertencia dura (los 6 sitios crashean headless; reglas nuevas selladas en la skill) y qué NO tomar (Rive como motor del sitio NO — solo piezas contenidas, BL-18). **→ ARRANCÁ ACÁ (B2): boceto de dirección de E0 (revamp de la intro supernova al guión — escena 0: la tienda, el colapso, el escape, el pulso naciendo) para gate de Alan ANTES de codear.** Leer primero: plan de operación + guión §0 y §Arquitectura + skill `pulsimus-escenas` + **fila E0 del mapa de referencias**. — Previo **2026-07-21 22:10 (CIERRE — pivote a ARQUITECTURA DE ESCENAS; commit selectivo SOLO docs+skills)**. 📖 **Guión narrativo canónico BENDECIDO** ([`bl17-guion-narrativo.md`](direccion/guion-narrativo.md)): la historia del protagonista (supernova destruye Y siembra → radar → cinturón de dolores → aterrizaje/tienda iso → órbitas → mission control → navegante → mandá tu señal), batería de conceptos-física, EKG leitmotiv en 4 apariciones; firme hasta Tablero, quien-soy/CTA revisables. + **§ Arquitectura de escenas (dirección de Alan)**: la página es una PELÍCULA de scroll — escenas full-viewport/umbral VARIABLE con el timing que cada momento merece, transiciones dirigidas escena↔sección, mapa E0–E7; **intro supernova a REVAMPEAR al guión**; nubes vuelven FLAT solo como escena del aterrizaje. 🎨 **Canon de generación** ([`canon-assets-higgsfield.md`](direccion/canon-assets.md)): flat+iso, "la luz es ámbar", offset ámbar consagrado, 4 tipos con specs, template maestro, anti-canon, workflow Alan-genera/cura → Claude-procesa/monta. **Tanda 1 generada por Alan y procesada** (pipeline sharp, Engram obs 350) → `public/space/` ~278KB. ⚠️ **TRABAJO SOBRE EL SITIO: SIN COMMITEAR A PROPÓSITO** (feedback de Alan: el montaje de validación no está a la altura; prefiere ORDENAR y corregir con gates pieza por pieza en sesión fresca antes de commitear código del sitio). En working tree: `SpaceDecor.tsx`, wiring en `page.tsx`, keyframes en `globals.css`, `StarLayer.tsx` SIN los bancos de nubes fotográficas (extirpados a pedido — mejoró TODO), `public/space/`. Detalle archivo por archivo en `Handoffs/handoff-2026-07-21-2215.md`. 🧰 **Skills**: creada **`pulsimus-escenas`** (`.claude/skills/` del repo, committeada — coreografía Disney→scroll + recetas GSAP + reglas/gotchas de la casa + checklist por escena); instaladas globales las OFICIALES de GreenSock `gsap-scrolltrigger` + `gsap-performance` (halladas con query simple — las compuestas las escondían) + `react-best-practices` y `web-design-guidelines` (Vercel). 🌍 Referencia validada por Alan: **journey.zajno.com** para secuencia/narrativa, aplicada al estilo Pulsimus (las demás no tienen su visión). **→ ARRANCÁ ACÁ (sesión nueva): (1) leer el handoff + skill `pulsimus-escenas`; (2) ORDENAR el trabajo visual del working tree CON gate de Alan pieza por pieza y recién ahí commitear; (3) después spike E2 (cinturón) punta a punta.** GAP mayor sin decidir: versión MOBILE de las escenas. Feedback de Alan sellado en memoria: gates visuales más frecuentes, respuestas más cortas. — Previo **2026-07-21 14:51 (CHECKPOINT mitad de sesión — pre-Fable)**. 🎬 **La supernova de Design es ahora LA INTRO del sitio** (cambio de plan vs el porteo a U4 que proponía el cierre previo): [`Intro.tsx`](../app/components/Intro.tsx) reescrita como autoplay del set-piece acreción→supernova (motor canvas fiel al HTML, `autoplaySecs=8`, clímax `p=0.66`; gates: 1×/sesión + reduced-motion + skip click/tecla + override `?intro` para replay; bloquea scroll; al terminar revela el hero). La intro vieja (pulse-travel + FLIP del símbolo al header) preservada íntegra en [`Intro.legacy.tsx`](../app/components/Intro.legacy.tsx). Verificado Playwright (arco vacío→acreción→clímax→explosión→wordmark→reveal hero; overlayGone + heroOpacity=1). Build PROD limpio. 🔗 **Design (Higgsfield) enlazado al repo GitHub por Alan** para que trabaje sobre el sitio real: se commiteó TODO el BL-17 del día (`342d701` feat(bl17)) y se pusheó a `origin/bl13-quien-soy` — **Design apunta a ESA rama**. **`main` INTACTO** (auto-deploya a prod → NO se mergea trabajo no gateado; el merge sigue siendo gate EXCLUSIVO de Alan). `.gitignore` += `/public/clouds/*.png` (43MB de placas PNG crudas de Higgsfield fuera del repo; el sitio solo usa `processed/` 2.6MB WebP). **→ ARRANCÁ ACÁ (Fable, post-compactación): PLANIFICAR cómo avanzar con la landing en MAYOR PROFUNDIDAD** (Alan switcheó a Fable — disponible hasta 50% de uso — para esta planificación). Insumos abiertos para el plan: gate visual del conjunto BL-17 pendiente · ¿la explosión SEA el reveal de quien-soy? · timing de la intro (8s ¿largo?) · gate P1 de quien-soy · verificar si el conector de Design lee la rama o solo `main`. — Previo **2026-07-18 20:25 (cierre sesión Fable — día de 4 gates de Alan sobre BL-17)**. 🌌 **La V1 estrella cambió de forma TRES veces con gates de Alan y quedó así**: (1) **pivot NOCHE-FIRST** (obs 335) — el sitio ES el cielo: murieron los fondos opacos de TODAS las secciones (reskin por ejecutor Sonnet 112k + fix cards Dolores a bg-hueso), SkyLayer (gradiente noche + 3 capas parallax de estrellas, z-negativo — gotcha de paint order documentado obs 336) corre continuo detrás, cards flotan. (2) **Motor v2: la estrella es COMPAÑERA DEL CURSOR** (obs 337) — el path por scroll murió ("pathing raro"); chase al puntero, órbita si no hay mouse, click en vacío = pulso, y la **onda-reveal construye cada sección al entrar al viewport naciendo DONDE está la estrella** (máscara .sw-pending en star.css; progressive enhancement: sin capa nada se oculta). (3) **Umbrales** (6, altura tuneable --umbral-h) con **escena de nubes REALES de Nano Banana** (obs 339): 10 placas de Alan procesadas (luminancia→alpha + feather + WebP ~250KB → public/clouds/processed/), 3 capas parallax ciclando variantes, banco frontal le abre un claro a la estrella; pipeline en scratchpad/process-clouds.ts. **Panel ?tune UNIFICADO con pestañas** (tuneRegistry + UnifiedTunePanel; los 4 paneles viejos BORRADOS). Verificación Playwright por fase (última 5/5 nubes; suites en scratchpad de sesión; gotchas: umbral alpha>150 para aislar estrella de nubes, fps headless = software rendering, no real). 🎁 **ENTREGADO SIN PORTEAR: `public/transitions/pulsimus-supernova.html`** — set-piece "supernova" de Claude Design (Ronda 2 de bl17-design-rounds.md): objeto TUNING, fases con clímax en 0.66, scrubbing determinista slider/scroll/autoplay, comentarios con instrucciones de porteo. **→ ARRANCÁ ACÁ (sesión nueva): portear la supernova como escena del umbral → quien-soy** (atada al progreso de scroll de SU banda, conviviendo con el reveal; interfaz pensada: escena por umbral `draw(ctx, rect, progress, starPos)`; el resto de umbrales quedan con nubes). Server de prod local corriendo en :3199 (matar con `fuser -k 3199/tcp` si molesta). TODO el trabajo del día SIN commitear en `bl13-quien-soy` (gate visual de Alan pendiente sobre el conjunto; gate P1 del quien-soy TAMBIÉN sigue abierto). Docs frescos: bl17-estrella-guion.md (3 addendums) + bl17-design-rounds.md (ronda 1 sellada). — Previo **2026-07-18 (CHECKPOINT mitad de sesión, pre-Fable)**. 🌟 **Dirección V1 redefinida con Alan**: narrativa **"estrella a nivel sitio"** (la estrella de Pulsimus existe quieta, un evento la acciona, viaja al scrollear, sus pulsos construyen las secciones, cierra con el logo — el scroll ES el viaje, el viaje ES el sitio construyéndose). **V1 sale en 2D + pseudo-3D** (Canvas 2D + GSAP ScrollTrigger + Lenis, capa aditiva ENCIMA de las secciones reales, NO rebuild); **Ferrari WebGL/Three.js = PRÓXIMA ÉPICA** (diferido por costo/foco: la landing aún no se muestra). **V1 completa = estrella 2D + completar quien-soy (BL-13) + los 3 skins (BL-16) + cierre animado narrativo.** Research verificó referencias (Zajno/Loopspeed/Igloo, todas WebGL) y DESCARTÓ el video horneado de Higgsfield (no interactúa con el DOM). 🛠️ **Riel P2 de quien-soy construido** (Sonnet: motor sticky+track + panel `?tune` QUIEN-SOY + fallback apilado; **en working tree SIN commitear**; capturas en `docs/gate-bl13/P2-andamiaje/`; hallazgo: el motion kit `data-rv` dispara los reveals de las 5 estaciones juntos → los efectos de entrada por estación necesitan mecanismo propio). Copy Intro ajustado ("afinar cada paso hasta la venta", sin OK final). **Gate P1 de quien-soy SIGUE pendiente.** **→ ARRANCÁ ACÁ (Fable): construir el piloto de la estrella en 2D/pseudo-3D; estimación ~450–800k/65–115min; sugerido spike Fase 1 (GSAP+Lenis+canvas SSR-safe en Next 16) para de-riskear.** Decisión en Engram obs 327. — Previo **2026-07-16 21:45** (cierre: 📬 **mail hola@pulsimus.dk FUNCIONANDO end-to-end** (DNS verificados por dig + prueba recibida en webmail; falta identidad Gmail, ver §Próximos pasos 2). 🧭 **BL-13 refinado a fondo CON Alan** (toki + 3 rondas de chat): evolucionó a **pieza scrollytelling de 5 estaciones full-viewport** con demos que DEMUESTRAN (skins/a-medida/sin-alma-con-alma); contrato sellado en backlog. ⚙️ **OpenSpec adoptado como SDD estándar** (decisión de Alan, todos los proyectos): init en este repo + cambio `bl13-quien-soy` autorado (proposal/spec/design/tasks, 15 tasks). 🏗️ **P1 CONSTRUIDA Y VERIFICADA** en rama `bl13-quien-soy` @ `4d20b40` pusheada (5 estaciones apiladas, gate en `docs/gate-bl13/`, ejecutor Sonnet 103k — piso del estimado): **esperando gate de Alan** (preview Vercel de la rama). Copy = borrador aprobado "mucho mejor" pero Alan lo macera; foto + URL LinkedIn = placeholders con TODO. Previo — 14/07 18:30: 🌐 **pulsimus.dk CONECTADO y sirviendo el sitio** — `www.pulsimus.dk` 200+SSL, apex 308→www, NS en Simply, DNSSEC on. **Mail comprado (Basic Mail) con `hola@` y `hello@` creadas pero SIN registros DNS de correo → los mails REBOTAN** hasta que Alan cargue los 4 registros (tabla en Próximos pasos §2; su intento falló por Tipo=AAAA en vez de MX). Antes, 17:25: (🚀 **F4R+F4T MERGEADOS Y LIVE EN PRODUCCIÓN** con OK explícito de Alan; replan del día: Prudence/EN baja, fixes chicos → paquete BL-12, nace BL-13 "Quién es Alan" (prioridad alta, a refinar), dominio comprado pendiente de aplicar a sitio+mail. Antes, 16:45: (sesión Fable: (1) **auditoría de conversión** de la landing vs ancla.digital/phinxlab + research competencia DK/ES/AR — hallazgo central: prod no tiene NINGUNA prueba (4 secciones, sin demo/casos/números); tesis validada por ausencia (nadie vende conversión con seña en 22 competidores); informes en `agency/research/competencia-agencias-2026-07-14.md`. (2) **BL-09 RESUELTO** en `f4-tablero` @ `fa6c6a7` pusheada: tablero 1590→860px (layout 3+3, fila 1 = paneles del toggle) + flash ámbar al togglear; verificado en build de PROD (58/58, eco end-to-end, mobile, reduced); addendum + capturas en `gate-f4t/`. (3) Backlog: BL-01 enriquecido con referencias parallax y subido a alta; BL-11 nuevo (video de fondo en loop, a refinar). Plan acordado: cerrar ciclo F4R/F4T y mergear ANTES de todo lo demás → después BL-01 → después decisión prueba+precio. Previo — 2026-07-09 23:42 (Alan hizo una **primera revisión en dev local** de ambos gates: *le gusta en general*, pero dejó feedback → BL-09 (densidad del tablero: que entre sin scroll, el toggle Día/Semana/Mes actualiza datos fuera del viewport) y BL-10 (mejoras del lado La Espiga/mostrador, sin especificar). **QA fino de Alan → mañana 2026-07-10.** Antes ~18:35: F4T CONSTRUIDO en `f4-tablero` (El tablero — 6 paneles + toggle + coreografía + `?tune`, verificado Playwright sobre build de PROD incl. **eco Reservas→mostrador**; gate en `gate-f4t/`). Antes ~14:45: F4R construido en `f4-mostrador`).

## Qué es

Landing de la agencia Pulsimus ("El pulso de tu negocio"). Repo `Schumyks/pulsimus` **anidado** en `Agencia/pulsimus/` (git propio; `Agencia` NO es git).
Stack: **Next 16 + Tailwind v4** (`@theme inline`) + Outfit + tokens "Faro Ámbar" (noche `#1B2140` / hueso `#F6EFE1` / ámbar `#F2A63E`; símbolo pulso→estrella).
Riel **GitHub → Vercel**: cada push a `main` auto-deploya a `pulsimus.vercel.app`. La app es donante 1 del starter (NO se construye sobre él).

## Estado de ramas / gates

- **`main` (1a2ab73)** — **LIVE en producción (merge 21/07 ~23:15 con autorización explícita de Alan; verificado en `pulsimus.vercel.app` Y `pulsimus.dk`)**: TODO el trabajo hasta el fix DPR — night-first + estrella-cursor (chase 8) + intro supernova + umbrales + quien-soy P1 (foto placeholder, Alan busca la definitiva) + base B0 limpia. La página aún NO se muestra activamente a nadie.
- **`bl13-quien-soy` (`31d476d`, +3 sin pushear a origin)** — rama de trabajo, **8 commits adelante de main** (B1 scrollEngine + docs BL-17 + reorg docs + skill `asset-vectorizer`). ⚠️ el nombre quedó chico (lleva todo BL-17 → candidato a renombrar `bl17-escenas`; toca el conector de Design, gate de Alan). Working tree (sesión 24/07 noche, **intencional**): `M` skill `asset-vectorizer` (edits research 2, POST-commit `31d476d`) · `M` STATE/backlog · `??` `direccion/tecnicas-mapeo-verificacion-research.md` → van en el commit de ESTE cierre. Aparte, `M`/`??` de sesiones previas SIN decidir (no tocados esta sesión): `pipeline-assets.md`, `vectorizacion-research.md`, `escenas/e0-supernova/spike-rive.md`. (El montaje intro-video ya NO figura en git → resuelto en sesión previa.) Cambio OpenSpec bl13: `openspec/changes/bl13-quien-soy/` (P1 en prod; P2 se replanifica bajo escenas).
- **`f4-mostrador` / `f4-tablero`** — ya mergeadas a `main` (merge `3de43f1`). Redundantes; seguras de borrar cuando Alan quiera (junto con `f3-motion` y `f3-pulse-proto`, redundantes desde antes).

## Gate en curso

**GATES F3 / F4R / F4T · ✅ CERRADOS Y EN PRODUCCIÓN** (F4R/F4T: merge 14/07 por OK de Alan; reports en `gate-f4r/` y `gate-f4t/` — este último con addendum BL-09). Veredictos que Alan dio el 14/07: layout 3+3 ✓ · flash del toggle ✓ · copy h2/sub y forma default de Pagos quedaron implícitamente OK con el "deployar tal como está". Fixes chicos detectados en su QA → **BL-12** (paquete, no de a uno).
**Nota**: previews por rama protegidos; producción NO.

## Próximos pasos (landing) — replan de Alan del 14/07

1. **→ ARRANCÁ ACÁ (CONSTRUCCIÓN del toolkit — el método YA está endurecido, ahora se construye):** construir + probar CONTRA LA TIENDA los scripts de la deuda de la skill `asset-vectorizer` (§ Deuda + § Verificación reforzada): **`map-pieces.mjs`** (bboxes por regionprops/connected-components → mata el mapeo a ojo) · **`score.mjs` reforzado a MULTI-EJE** (ΔE + SSIM + IoU + FSIM/GMSD + Hausdorff, pooling p95, `--mask` por target) · **`crop-check.mjs`** (anillo perimetral + residuo) · **`check-rig.py`** (`xmllint` + paths anónimos). Restricción de Alan: **NO regenerar assets** — usar los individuales que YA existen (`space-src/…/spike-rive/tienda/pieces/` + `piezas_vec/`) + el composite como stress-test. Prioridades en [`direccion/tecnicas-mapeo-verificacion-research.md`](direccion/tecnicas-mapeo-verificacion-research.md) § 'Qué se aterriza'. **Histórico (hecho):** TEST DEL MARCO (carita de cero → `face_smile_3.svg`, norma base 8.31) ✓ · stress-test TIENDA (`tienda_vector.svg` gateado, correcciones techo/toldos/buzón pendientes) ✓. Pipeline y frontera 🎨/⚡ en [`direccion/pipeline-assets.md`](direccion/pipeline-assets.md). **Histórico (hecho):** Revisar CON Alan sus boards E0 de Midjourney y cerrar TRES decisiones: (a) estilo ancla definitivo (Kurzgesagt-MJ vs flat Higgsfield tanda 1 — un solo dueño del look; si gana MJ, el canon actualiza referencia); (b) canal de la intro E0: video (animatic MJ / Higgsfield, validar resolución de export y peso) vs canvas adaptado — recomendación híbrida en [`bl17-boceto-e0.md`](escenas/e0-supernova/boceto.md) § Addendum; (c) ¿la carita de la tienda entra al canon del elenco? Con eso decidido → task 1.2/1.3 del OpenSpec (tanda E0 final + build intro) o, si Alan sigue generando, task 2.1 (boceto E1 radar). Insumo pendiente de Alan (no bloquea): foto real para quien-soy.
2. **Mail del dominio — ✅ FUNCIONANDO end-to-end (16/07):** registros DNS verificados vía `dig` (MX `10 mx.simply.com` + SPF + DKIM 1/2; A/CNAME del sitio intactos) y **mail de prueba recibido en la bandeja de `hola@` en webmail.simply.com** (login: dirección completa + contraseña de la casilla). Falta cerrar:
   1. **Identidad Gmail SEPARADA (decisión de Alan 16/07: no reenviar a su cuenta personal; lo setea él "más tarde"):** crear cuenta Google nueva de Pulsimus (opción A recomendada, gratis; ej. `pulsimus.dk@gmail.com`, nadie la ve) → reenvío en Simply `hola@` → esa cuenta → "Enviar como" `hola@pulsimus.dk` vía `smtp.simply.com:587` (usuario = dirección completa, contraseña de la casilla, TLS). `hello@`: solo reenvío, sin "Enviar como". Workspace (~75 DKK/mes) descartado por ahora.
   2. **Opcional (recomendado antes de mandar mails salientes):** TXT `_dmarc` = `v=DMARC1; p=none; rua=mailto:hola@pulsimus.dk` — aún no cargado (verificado 16/07). Webmail mientras tanto: webmail.simply.com (dirección completa + contraseña de la casilla).
3. **BL-12 · paquete de pulido** (acumulando): scroll interno de la cola Reservas (bug) + "TICKET PROM." a 1 línea + ítems BL-10 cuando bajen + limpieza `?tune`/TunePanel + borrar ramas redundantes.
4. **BL-01 · fondo vivo** (alta, refinar antes de construir) y **BL-11 · video loop** (media) — la capa "vida" post-auditoría.
5. **Prudence/módulo EN: BAJADO por Alan (14/07).** F5 (Cal.com + forms) sigue bloqueada por prerequisitos.

## Pendientes F5 (prerequisitos de Alan)

~~Dominio~~ ✅ **comprado (14/07)** — falta aplicarlo (DNS → Vercel) y el mail `hola@`. Backorder `pulsimus.com` (vence 02/08) · cuenta Cal.com · servicio de forms. **Créditos Higgsfield → FG, NO landing.**

## Decisiones de la landing (02/07 — detalle en [../../agency/landing-brief.md](../../agency/landing-brief.md) + Engram `pulsimus/landing-*`)

Posicionamiento "C disfrazada de A" (categoría web, promesa conversión) · página = credencial post-pitch · diferenciador "los dos lados del mostrador" · 7 secciones (stack eliminada; slot Ejemplos vivo) · pieza firma El mostrador (panadería ficticia **La Espiga**, alcance CONGELADO) · CTA primario Cal.com + secundario form→mail · solo ES voseo en v1 · Next.js+Tailwind+Vercel en `pulsimus.dk` · glosario en `CONTEXT.md` (raíz del repo).
**Instagram híbrido:** "Instagram" (concreto) en la línea-firma; "las redes" (genérico) en la voz del dueño.

## Registro / decisiones previas

- [`f2-voz-revision.md`](archivo/landing-secciones/voz-revision.md) — voz + Instagram híbrido.
- [`f4-image-prompts.md`](archivo/mostrador-tablero/image-prompts.md) — assets: curación de Alan = fuente de verdad, pipeline rembg local, **demo = CÓDIGO jamás video**.
- [`backlog.md`](backlog.md) — backlog de la landing reorganizado por fase del roadmap (fuente única de parqueos).
- [`f3-intro-donante.md`](archivo/intro/donante.md) — coreografía completa de la intro extraída del donante v1.

## Memoria (Engram)

Project `agencia` fijado por **DOS** configs: el de la raíz de Agencia Y [`.engram/config.json`](../.engram/config.json) **en este repo** (commiteado). Aprendido el 09/07: el config de la raíz NO alcanza cuando la sesión se abre con cwd EN este repo hijo — la resolución cae a `git_remote` → project fantasma `pulsimus` (le pasó a `mem_session_summary`, que no acepta `project=`). Con el config local queda cerrado (verificado: `mem_current_project` → `source: config`). Pasar `project=agencia` explícito sigue siendo el respaldo en las tools que lo aceptan. Huérfano residual documentado: obs #173 (tombstone con puntero). Registro: F3 id 186, F4 id 187; fixes de memoria id 194/195 y migración obs #216 (09/07).
